#include <cstdio>
#include <assert.h>
#include <iostream>
#include <algorithm>
#include <cmath>
#include <cstring>
#include <string>
#include <stack>
#include <queue>
#include <vector>
#include <utility>

// g++ -std=c++14 -stdlib=libc++ -O2 -o s.out 2.cpp && ./s.out < x.txt

using namespace std;
using ll = long long;
// void solve() {
//     for (int i = 0; i < n; i++) {
//         for (int j = 1; j <= 40; j++) {
//             for (int k = 0; k < n; k++) {
//                 for (int x = 0; x < (1 << n)-1; x++) {
//                     for (int jj = 1; jj <= 40; jj++) {
//                         if (j%jj == 0 && jj used in x && j used in x) {
//                             y = turn off j in x
//                             dp[i][j][k][x] += dp[i-1][jj][k-1][y]
//                         }
//                     }
//                 }
//             }
//         }
//     }
// }
int main() {
    
    return 0;
}