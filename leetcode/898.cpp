#include <cstdio>
#include <iostream>
#include <algorithm>
#include <cmath>
#include <cstring>
#include <string>
#include <stack>
#include <queue>
#include <vector>
#include <utility>
#include <map>

// g++ -std=c++14 -stdlib=libc++ -O2 -o s.out 1000.cpp && ./s.out

using namespace std;
int solve(vector<int> a) {
    int n = (int)a.size();
    for (int i = 0; i < n; i++) {
        map<int, bool> m = {{a[i], true}};
        for (int j = i+1; j < n; j++) {
            int k = 
        }
    }
}
int main() {
    
    return 0;
}